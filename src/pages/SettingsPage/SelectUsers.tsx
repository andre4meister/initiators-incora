import { FC, useState } from 'react';

import Select, { ActionMeta, OnChangeValue, StylesConfig } from 'react-select';
import { User } from 'types/dataTypes';

const SelectUsers: FC = () => {};

export default SelectUsers;
